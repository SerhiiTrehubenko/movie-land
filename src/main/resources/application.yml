spring:
  flyway:
    enabled: true
    locations: classpath:db.migration
    baseline-on-migrate: true

  datasource:
    username: postgres
    password: password
    url: jdbc:postgresql://192.168.0.169:5432/movie_land
    driver-class-name: org.postgresql.Driver

genre.cache.cycle.hours: 4
movie.number.random: 3
