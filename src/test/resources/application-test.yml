spring:
  flyway:
    enabled: true
    locations: classpath:db.migration #When it is removed flyway does not migrate
    baseline-on-migrate: true

  datasource:
    username: postgres
    password: password
    url: jdbc:postgresql://localhost:5432/movie_land_dev
    driver-class-name: org.postgresql.Driver

genre:
  cache:
    cycle:
      hours: 4
number:
  movies:
    random: 3